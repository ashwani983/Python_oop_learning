name ="Jim" #str
print(len(name))

some_lst = ["one","two","three","four"]
print(len(some_lst))

# This is a polymorphism example
# len() is a built-in function that returns the length of an object.
# The len() function works with sequences like strings, lists, and tuples.
